BalancesTableIAM:
  Effect: Allow
  Action:
    - dynamodb:PutItem
    - dynamodb:Scan
    - dynamodb:GetItem
    - dynamodb:UpdateItem
    - dynamodb:Query
  Resource:
    - ${self:custom.BalancesTable.arn}
    - !Join ['/', ['${self:custom.BalancesTable.arn}', 'index', 'byUserId']]
